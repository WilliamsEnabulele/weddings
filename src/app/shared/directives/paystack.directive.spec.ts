import { PaystackDirective } from './paystack.directive';

describe('PaystackDirective', () => {
  it('should create an instance', () => {
    const directive = new PaystackDirective();
    expect(directive).toBeTruthy();
  });
});
